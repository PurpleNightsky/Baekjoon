#include <stdio.h>
#include <iostream>

using namespace std;

#define _CRT_SECURE_NO_WARNINGS

int main() {  
    int N;
    scanf("%d", &N);
    printf("%d", N * (N - 1));
    return 0;
}
